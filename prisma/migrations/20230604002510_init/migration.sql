-- CreateTable
CREATE TABLE "nombreModel" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "price" INTEGER NOT NULL,

    CONSTRAINT "nombreModel_pkey" PRIMARY KEY ("id")
);
